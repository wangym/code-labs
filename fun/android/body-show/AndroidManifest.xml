<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="me.yumin.android.bodyshow.activity" android:versionCode="1" android:versionName="1.0">

	<!-- activity -->
	<application android:icon="@drawable/icon" android:label="@string/app_name">
		<activity android:name=".LoadingActivity" android:theme="@style/fullscreen">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>
		<activity android:name=".LoginActivity" android:theme="@style/noTitlebar" />
		<activity android:name=".PostActivity" android:theme="@style/noTitlebar" />
		<activity android:name=".RegisterActivity" android:theme="@style/noTitlebar" />
		<activity android:name=".ShowActivity" android:theme="@style/noTitlebar" />
	</application>
	<!-- other -->
	<supports-screens android:anyDensity="true" android:smallScreens="true" android:normalScreens="true" android:largeScreens="true" />
	<uses-sdk android:minSdkVersion="4" android:targetSdkVersion="4" />
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

</manifest>
